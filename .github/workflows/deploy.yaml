name: Deploy to AWS

on:
  push:
    branches: [cloud-formation-demo]

env:
  AWS_REGION: ap-southeast-1
  
jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS credentials using OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: "${{ vars.ACTION_ROLE }}"
          role-session-name: samplerolesession
          aws-region: ${{ env.AWS_REGION }}

      - name: Print assumed role
        run: aws sts get-caller-identity
      # - name: Install dependencies (replace with your build steps)
      #   run: npm install # Or your specific build command

      # - name: Build and deploy to AWS (replace with your deployment commands)
      #   run: npm run build && aws s3 cp --recursive build s3://your-bucket-name/ # Or your deployment commands


